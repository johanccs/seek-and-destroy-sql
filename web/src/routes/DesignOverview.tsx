export default function DesignOverview() {
  return (
    <div className="empty">
      <div>
        <h2>Database Design</h2>
        <p className="muted">
          Design and model schemas on an interactive canvas, then build them for real against a live
          SQL Server — normalization, relationships, indexing strategy, and the trade-offs behind them.
        </p>
        <p className="muted startup-note">
          🚧 <strong>Under construction.</strong> The module curriculum and the ERD canvas are being
          built out. The SQL Performance track is unaffected.
        </p>
      </div>
    </div>
  );
}
