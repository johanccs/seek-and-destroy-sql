{
  "id": "b-15-range-predicate-needs-index",
  "level": "beginner",
  "order": 15,
  "title": "Range Queries Need an Index Too",
  "description": "A BETWEEN range on an unindexed column scans; a B-tree index serves ranges as well as equality, seeking straight to the band. Why it matters: range filters (dates, prices, IDs) are everywhere in real screens, and this exact gap is why a report that's fast today grinds to a halt as the table grows.",
  "topics": [
    "indexing",
    "range-predicate",
    "seek-vs-scan"
  ],
  "estimatedMinutes": 10,
  "narrative": "## What This Lesson Is About\nThis lesson is about **range queries** — filters like `BETWEEN`, `>`, or `<` that ask for a band of values rather than one exact match — and why they need an index just as much as an exact-match lookup does, even though it's easy to assume ranges are \"harder\" to index.\n\nWhy does this happen? Without an index on the filtered column, SQL Server has no way to know which rows fall inside your range without checking every single row's value against the boundaries. It's not that ranges are inherently unindexable — it's simply that no index exists here at all, so there's no shortcut of any kind, exact-match or range.\n\nPicture a stack of employee files with no particular order, and someone asks \"find me every employee whose salary is between $45,000 and $50,000.\" With no sorted structure to rely on, you have to check every single file's salary field one at a time. Now imagine the same files sorted by salary — suddenly you can flip straight to the $45,000 mark and read forward until you pass $50,000, then stop. That's exactly what an index does for a range query: it doesn't just help you find one exact value, it lets you find a whole contiguous band of values by jumping to the start and reading only as far as you need.\n\n## Scenario\nAn HR screen lists employees in a narrow salary band. There's no index on `Salary`, so finding ~170 people forces SQL Server to read all 200,000 rows.\n\n## Why it's slow\nA `BETWEEN` is a **range predicate**. Without a suitable index, the only way to satisfy it is a **Clustered Index Scan** — read every row and test each against the range. It works, but the cost grows with the whole table, not with how many rows match.\n\n## What to look for\n- A **Clustered Index Scan** of `Employees` with reads proportional to the full table.\n- A tiny result set returned only after a full scan.\n\n## The concept\nAn index isn't just for equality (`=`) lookups — because a B-tree keeps keys **in sorted order**, it's ideal for ranges (`BETWEEN`, `>`, `<`). Add a nonclustered index on `Salary` (INCLUDE the columns you return so it's covering) and the engine **seeks** to the start of the band and walks just the matching rows.",
  "startingQuery": "SELECT EmployeeId, Name, Salary\nFROM Employees\nWHERE Salary BETWEEN 149000 AND 149100;",
  "hints": [
    "There is no index on Salary, so the range test must scan every row.",
    "A B-tree index stores keys in order, which makes it perfect for BETWEEN / > / < ranges, not just equality.",
    "CREATE NONCLUSTERED INDEX IX_Employees_Salary ON Employees(Salary) INCLUDE (Name); then re-run the query."
  ],
  "passConditions": [
    {
      "type": "requireOperator",
      "operator": "Index Seek"
    },
    {
      "type": "noOperator",
      "operator": "Clustered Index Scan"
    },
    {
      "type": "resultUnchanged"
    }
  ]
}
