{
  "id": "b-09-count-narrow-index",
  "level": "beginner",
  "order": 9,
  "title": "Counting Rows: Pick the Narrowest Index",
  "description": "COUNT(*) over a wide clustered index reads huge rows; a narrow nonclustered index gives the engine far fewer pages to scan.",
  "topics": [
    "indexing",
    "io",
    "aggregation"
  ],
  "estimatedMinutes": 10,
  "database": "Lesson_b_09_count_narrow_index",
  "narrative": "## Scenario\nA dashboard runs `SELECT COUNT(*) FROM Orders` on a 200,000-row table. It feels heavy for something that just returns one number.\n\n## Why it's slow\n`COUNT(*)` has to visit **every row once**, but it can do that through *any* index — it doesn't care which columns are there, only how many rows exist. The only structure available here is the **clustered index**, and each row carries a `CHAR(200)` `Filler` column, so the rows are wide and the clustered index spans **thousands of 8 KB pages**. A `Clustered Index Scan` therefore reads them all.\n\n## What to look for\n- In the plan: a **Clustered Index Scan** on `Orders`.\n- In `STATISTICS IO`: **logical reads** in the thousands just to count rows.\n\n## The concept\nAdd a **narrow nonclustered index** — say on the 1-byte `Region` column. Every row still appears exactly once in that index, so the count is unchanged, but the index is tiny compared to the wide clustered rows. The optimizer automatically picks the **smallest** index that can answer the query, turning the scan into a far cheaper `Index Scan` and cutting logical reads dramatically.",
  "startingQuery": "SELECT COUNT(*) FROM Orders;",
  "hints": [
    "COUNT(*) can use any index — the optimizer picks the smallest one that covers the query.",
    "The only index here is the clustered index, and the CHAR(200) Filler makes every row wide.",
    "Create a skinny nonclustered index (e.g. on Region) so COUNT(*) scans it instead: CREATE NONCLUSTERED INDEX IX_Orders_Region ON dbo.Orders(Region);"
  ],
  "passConditions": [
    {
      "type": "noOperator",
      "operator": "Clustered Index Scan"
    },
    {
      "type": "requireOperator",
      "operator": "Index Scan"
    },
    {
      "type": "maxLogicalReads",
      "table": "Orders",
      "value": 1500
    },
    {
      "type": "resultUnchanged"
    }
  ]
}
